<div class="header-row">
    <h3>Vaccines</h3>
    <a class="btn btn-primary" href="/certificate/{{cert_id}}/vaccinesAddOrEdit">Create New</a>
</div>
<table class="table table-striped">
    <thread>
        <tr>
            <th>#</th>
            <th>Brand</th>
            <th>Date</th>
            <th>Covid Center</th>
            <th>Health Worker</th>
            <th>Actions</th>
        </tr>
    </thread>
    <tbody>
        {{#each vaccines}}
        <tr>
            <td>{{@index}}</td>
            <td>{{this.brand}}</td>
            <td>{{this.date}}</td>
            <td>Name: {{this.covid_center.name}}<br>
            Address: {{this.covid_center.address}}<br>
            Type: {{this.covid_center.center_type}}<br></td>
            <td>First Name: {{this.health_worker.first_name}}<br>
            Last Name: {{this.health_worker.last_name}}<br>
            Phone Number: {{this.health_worker.phone_number}}<br>
            Email: {{this.health_worker.email}}</td>
            <td>
                <div class="actions">
                    <a class="btn btn-primary btn-sm mr-2" href="/certificate/{{@root.cert_id}}/vaccinesAddOrEdit/{{this._id}}">Edit</a>
                    <a class="btn btn-primary btn-sm" href="/certificate/{{@root.cert_id}}/vaccine/{{this._id}}/delete"
                        onclick="return confirm('Are you sure you want to delete this record?');">Delete</a>
                </div>
            </td>
        </tr>
        {{/each}}
    </tbody>
</table>

